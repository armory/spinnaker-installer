#!/bin/bash

# This script creates the install script artifact.
# WARNING: This script should be executed via the arm tool.

set -e
cd "$(dirname "$0")"

rm -dfr ${BUILD_DIR}
mkdir -p ${BUILD_DIR}

ARTIFACT_NAME="install-${GIT_HASH}.sh"

cp `pwd`/src/install.sh `pwd`/build/${ARTIFACT_NAME}
